Things for the exam:

- C programming
	- Bitwise math: XOR, OR, AND, etc.
	- Pointers: Difference between pointer to data and the actual data, difference between pointer and array marker
	- Function pointers: how they work, what happens if function pointer is NULL/garbage data, where are they use

- Q2 specifics:
	- How entity, items and clients are defined
	- How behaviors are differentiated between entities
	- How Q2 defines and uses vectors
	- Parsing user commands, UI, user obituary
	- What "gi" does
	- How hitscan works
	- How we link entities on the map to that entities in the code
	- What's a temporary entity
	- "Conceptually, what is an entity?"
	- How are user commands mapped to in-game actions
	
